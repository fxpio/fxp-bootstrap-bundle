{% block media_list_widget -%}
{%- if rendered -%}
    {%- require_style '@bower/bootstrap/less/bootstrap.less' -%}
    {%- set attr = attr|merge({'class': ('media-list ' ~ attr.class|default(''))|trim}) -%}

    <ul{{ block('widget_container_attributes') }}>
        {{- value|raw -}}
        {%- for child in block -%}
            {{- block_widget(child) -}}
        {%- endfor -%}
    </ul>
{%- endif -%}
{%- endblock media_list_widget %}


{% block media_widget -%}
{%- if rendered -%}
    {%- set attr = attr|merge({'class': ('media ' ~ attr.class|default(''))|trim}) -%}

    <{{ tag }}{{ block('widget_container_attributes') }}>
    {{- value|raw -}}

    {%- for child in block -%}
        {{- block_widget(child) -}}
    {%- endfor -%}

    </{{ tag }}>
{%- endif -%}
{%- endblock media_widget %}


{% block media_body_widget -%}
{%- if rendered -%}
    {%- set attr = attr|merge({'class': ('media-body ' ~ attr.class|default(''))|trim}) -%}

    <div{{ block('widget_container_attributes') }}>
        {%- if label is not empty %}<h4 class="media-heading">{{ label|block_trans({}, translation_domain) }}</h4>{% endif -%}
        {{- value|raw -}}
        {%- for child in block -%}
            {{- block_widget(child) -}}
        {%- endfor -%}
    </div>
{%- endif -%}
{%- endblock media_body_widget %}
